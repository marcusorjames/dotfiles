$terminal = uwsm-app -- xdg-terminal-exec
bindd = SUPER ALT, RETURN, Tmux, exec, uwsm-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)" tmux new
$browser = omarchy-launch-browser

# Unbind defaults
unbind = SUPER, W # Unbind default close action

# Application
bindd = SUPER, RETURN, Terminal, exec, $terminal --working-directory="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER SHIFT, RETURN, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- nautilus --new-window "$(omarchy-cmd-terminal-cwd)"
bindd = SUPER, E, Email, exec, omarchy-launch-or-focus "uwsm-app -- /opt/wavebox.io/wavebox/wavebox-launcher"
bindd = SUPER, W, Browser, exec, $browser
bindd = SUPER SHIFT, R, Reload hyperland, exec, hyprctl reload && notify-send "Hyprland reloaded" || notify-send "Failed to reload Hyprland"
bindd = SUPER SHIFT, M, Music, exec, toggle-special-workspace music && omarchy-launch-or-focus spotify "uwsm-app -- spotify --force-device-scale-factor=1.25"
bindd = SUPER SHIFT, N, Editor, exec, omarchy-launch-editor
bindd = SUPER SHIFT, B, Activity, exec, omarchy-launch-tui btop
bindd = SUPER SHIFT, T, Tasks, exec, omarchy-launch-or-focus todoist
bindd = SUPER SHIFT, D, Docker, exec, omarchy-launch-tui lazydocker
bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus "^obsidian$" "uwsm-app -- obsidian -disable-gpu --enable-wayland-ime"
bindd = SUPER SHIFT, W, Typora, exec, uwsm-app -- typora --enable-wayland-ime
bindd = SUPER SHIFT, SLASH, Passwords, exec, omarchy-launch-or-focus signal "uwsm-app -- bitwarden-desktop"

# Webapps
# --- If your web app url contains #, type it as ## to prevent hyprland treating it as a comment ---
bindd = SUPER SHIFT, A, Gemini, exec, omarchy-launch-or-focus-webapp Gemini "https://gemini.google.com/"
bindd = SUPER SHIFT, S, Slack, exec, toggle-special-workspace slack && omarchy-launch-or-focus-webapp Slack "https://bolder-org.slack.com"
bindd = SUPER SHIFT ALT, A, Claude, exec, omarchy-launch-or-focus-webapp Claude "https://claude.ai"
bindd = SUPER SHIFT, Y, YouTube, exec, omarchy-launch-webapp "https://youtube.com/"
bindd = SUPER SHIFT, C, WhatsApp, exec, toggle-special-workspace whatsapp && omarchy-launch-or-focus-webapp WhatsApp "https://web.whatsapp.com/"
bindd = SUPER ALT SHIFT, C, Discord, exec, toggle-special-workspace discord && omarchy-launch-or-focus-webapp Discord "https://discord.com/channels/@me"
bindd = SUPER SHIFT CTRL, G, Google Messages, exec, omarchy-launch-or-focus-webapp "Google Messages" "https://messages.google.com/web/conversations"

# Windows
unbind = SUPER, L
bind = SUPER, H, movefocus, l
bind = SUPER, J, movefocus, d
bind = SUPER, K, movefocus, u
bind = SUPER, L, movefocus, r
bindd = SUPER, Q, Hide scratchpad, exec, hide-scratchpad
bind = SUPER SHIFT, Q, killactive
unbind = SUPER, TAB
bind = SUPER, TAB, focuscurrentorlast

# Rebind layout (SUPER L) to SUPER ALT L
bindd = SUPER SHIFT, L, Toggle workspace layout, exec, omarchy-hyprland-workspace-layout-toggle

bindd = SUPER ALT, code:20, Shrink window up, resizeactive, 0 -100 # Rebind - key with alt for window shrink up
bindd = SUPER ALT, code:21, Expand window down, resizeactive, 0 100 # Rebind - key with alt for window shrink down

unbind = SUPER SHIFT, code:20 # Unbind - for window shrink up
unbind = SUPER SHIFT, code:21 # Unbind = for window shrink down

# Media
bindd = SUPER SHIFT, P, Pause, exec, omarchy-swayosd-client --playerctl play-pause
bindd = SUPER SHIFT, code:21, Volume up precise, exec, omarchy-swayosd-client --output-volume +1 # = key
bindd = SUPER SHIFT, code:20, Volume down precice, exec, omarchy-swayosd-client --output-volume -1 # - key
bindd = SUPER SHIFT, period, Next track, exec, omarchy-swayosd-client --playerctl next
bindd = SUPER SHIFT, comma, Previous track, exec, omarchy-swayosd-client --playerctl previous

